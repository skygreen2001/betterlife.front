'use strict';

// Register the `layoutFooter` component on the `tk.layout` module
angular.
    module('bb.component.layout').
    component('layoutFooter', {
        templateUrl: 'template/layout/footer/footer.template.html',
        controller: ['$scope',
        function($scope, $element, $attrs) {

        }]
    });
