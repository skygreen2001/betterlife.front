'use strict';

// Define the `bf article` module
angular.module('bb.component.common', []).
config(['$locationProvider', '$routeProvider',
    function config($locationProvider, $routeProvider) {

    }
]);
